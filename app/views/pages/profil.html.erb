<div class="content">
  <% if @user == current_user %>
    <h1> Mon profil </h1>
    <%= current_user.first_name %> <%= current_user.last_name %>
  <% else %>
    <%= @user.first_name %> <%= @user.last_name %>
  <% end %>
</div>
